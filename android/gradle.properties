# Propiedades del build raíz (composite build).
# Cada proyecto incluido usa su propio gradle.properties (apps/<app>/android/gradle.properties).

org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
